// Public Supabase project settings. The anon key is safe to publish when the
// row-level security policies in supabase-admin.sql are enabled.
window.KICKBALL_ADMIN_CONFIG = {
  supabaseUrl: "",
  supabaseAnonKey: ""
};
