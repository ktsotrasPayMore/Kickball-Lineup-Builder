# Repository instructions

- Do not add plain-text (`.txt`) files. Put durable documentation in an appropriate Markdown file instead.
- Remove any `.txt` files introduced by a change before committing it.
